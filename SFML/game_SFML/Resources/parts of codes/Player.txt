// Player initialization
// Player-----------------------------------------------------------
sf::Texture playerTexture("D://Programs/Visual Studio/VS_Projrcts/game_SFML/Resources/player.png");
Creature player(playerTexture);

//In poll event
if (sf::Keyboard::isKeyPressed(sf::Keyboard::Key::Space) && !player.getJumping()) {
	clck.restart();
	player.setJumping();
}

// In main while loop
if(player.getJumping())
	player.jump(clck);

if (sf::Keyboard::isKeyPressed(sf::Keyboard::Key::D)) {
	player.move({ 0.5f, 0.f });
}
if (sf::Keyboard::isKeyPressed(sf::Keyboard::Key::A)) {
	player.move({ -0.5f, 0.f });
}

window.draw(player);